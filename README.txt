Alt+Shift+F4 (Abre la terminal en esta carpeta)

sudo -s

git status (información sobre el repo)

git add . (añade todos los archivos nuevos o actualizados. En vez del "." puede usarse el nombre de un archivo particular)

git commit -m "MENSAJE"

git push (sube los cambios al servidor)
